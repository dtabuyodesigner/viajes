{
 "_pagina": "<header> <div id=\"tema-zona\"><div class=\"tema-mini\"><button class=\"tm\" data-tema=\"auto\" aria-label=\"Automático\" title=\"Automático\">◐</button><button class=\"tm\" data-tema=\"claro\" aria-label=\"Claro\" title=\"Claro\">☀</button><button class=\"tm on\" data-tema=\"oscuro\" aria-label=\"Oscuro\" title=\"Oscuro\">☾</button></div></div> <a class=\"volver\" href=\"../\">‹ Viajes</a> <h1 id=\"titulo\">Viaje de prueba</h1> <p class=\"sub\" id=\"sub\">Editando un viaje que ya tenías.</p> </header> <main> <div class=\"pedir\"> <div class=\"pedir-cab\"> <span class=\"chispa\">✦</span> <div><b>Que te lo monte una IA</b><small>Le dices dónde y cuántos días, y ella rellena el viaje</small></div> </div> <button class=\"btn solid\" id=\"abrir-pedir\" style=\"width:100%;margin-top:12px\">Preparar la petición</button> <div id=\"zona-pedir\"></div> </div> <div class=\"campo\"> <label>Nombre del viaje</label> <input type=\"text\" id=\"nombre\" placeholder=\"Portugal en furgo\" autocomplete=\"off\"> </div> <div class=\"dos\"> <div class=\"campo\"><label>Salida</label><input type=\"date\" id=\"desde\"></div> <div class=\"campo\"><label>Vuelta</label><input type=\"date\" id=\"hasta\"></div> </div> <p class=\"nota\" style=\"margin-top:-6px\">Si aún no las sabéis, déjalas en blanco: los días saldrán como «Día 1», «Día 2»…</p> <div class=\"campo\" style=\"margin-top:16px\"> <label>Punto de partida</label> <input type=\"text\" id=\"salida\" placeholder=\"San Miguel de las Dueñas\" autocomplete=\"off\"> </div> <p class=\"sec\">Los días</p> <div id=\"dias\"> <div class=\"dia\" data-dia=\"0\"> <div class=\"dia-cab\"> <span class=\"dia-num\">1</span> <h2>Día uno</h2> <button class=\"btn fino borrar\" data-borrar-dia=\"0\">Quitar</button> </div> <div class=\"campo\"> <label>Qué se hace ese día</label> <input type=\"text\" class=\"d-t\" data-i=\"0\" value=\"Día uno\" placeholder=\"Subida a Somiedo\"> </div> <div class=\"campo\"> <label>Dónde se duerme</label> <input type=\"text\" class=\"d-dest\" data-i=\"0\" value=\"Zagreb\" placeholder=\"Pola de Somiedo\"> </div> <label style=\"font-family:var(--mono);font-size:10.5px;letter-spacing:.18em; text-transform:uppercase;color:var(--tinta-dim);display:block;margin:14px 0 7px\">Paradas</label> <div class=\"parada\"> <div class=\"fila\"> <input type=\"text\" class=\"mini p-h\" data-i=\"0\" data-k=\"0\" value=\"Mañana\" placeholder=\"Mañana\"> <input type=\"text\" class=\"mini p-txt\" data-i=\"0\" data-k=\"0\" value=\"Una parada\" placeholder=\"Qué se visita\"> <button class=\"btn fino borrar\" data-borrar-par=\"0:0\">×</button> </div> <input type=\"text\" class=\"mini p-n\" data-i=\"0\" data-k=\"0\" value=\"Una nota\" placeholder=\"Nota: horarios, avisos…\" style=\"margin-top:7px\"> <input type=\"text\" class=\"mini p-mapa\" data-i=\"0\" data-k=\"0\" value=\"Zagreb\" placeholder=\"Nombre para el navegador\" style=\"margin-top:7px\"> </div> <div class=\"btns\"> <button class=\"btn fino\" data-add-par=\"0\">+ Parada</button> <button class=\"btn fino busca\" data-buscar=\"0\" data-l=\"1\">Buscar un sitio</button> </div> <div class=\"buscador-zona\" id=\"bz-0\"></div> <label style=\"font-family:var(--mono);font-size:10.5px;letter-spacing:.18em; text-transform:uppercase;color:var(--tinta-dim);display:block;margin:16px 0 7px\">Foto del día</label> <label class=\"subir\"> <input type=\"file\" accept=\"image/*\" data-foto=\"0\" hidden=\"\" data-l=\"1\"> <span>+ Añadir una foto</span> </label> </div></div> <div class=\"btns\"><button class=\"btn\" id=\"add-dia\">+ Añadir día</button></div> <p class=\"sec\">Reservas</p> <p class=\"nota\" style=\"margin:0 0 12px\">Lo que quieras tener a mano sin buscar en el correo.</p> <div id=\"reservas\"> <div class=\"dia\"> <div class=\"dia-cab\"> <span class=\"dia-num\">✈</span> <h2>MAD → ZAG</h2> <button class=\"btn fino borrar\" data-borrar-vuelo=\"0\">Quitar</button> </div> <div class=\"campo\"><label>Trayecto</label> <input type=\"text\" class=\"v-ruta\" data-i=\"0\" value=\"MAD → ZAG\" placeholder=\"Madrid MAD → Zagreb ZAG\"></div> <div class=\"dos\"> <div class=\"campo\"><label>Fecha</label> <input type=\"text\" class=\"v-fecha\" data-i=\"0\" value=\"1 sep\" placeholder=\"18 jul\"></div> <div class=\"campo\"><label>Hora</label> <input type=\"text\" class=\"v-hora\" data-i=\"0\" value=\"18:55\" placeholder=\"18:55\"></div> </div> <div class=\"dos\"> <div class=\"campo\"><label>Compañía</label> <input type=\"text\" class=\"v-cia\" data-i=\"0\" value=\"X\" placeholder=\"Vueling\"></div> <div class=\"campo\"><label>Localizador</label> <input type=\"text\" class=\"v-loc\" data-i=\"0\" value=\"ABC123\" placeholder=\"ABC123\" autocapitalize=\"characters\"></div> </div> <div class=\"campo\"><label>Notas</label> <input type=\"text\" class=\"v-n\" data-i=\"0\" value=\"\" placeholder=\"Terminal 4, facturar equipaje…\"></div> </div><div class=\"dia\"> <div class=\"dia-cab\"><span class=\"dia-num\">🚗</span><h2>Coche de alquiler</h2></div> <div class=\"dos\"> <div class=\"campo\"><label>Empresa</label> <input type=\"text\" id=\"c-emp\" value=\"Rent\" placeholder=\"Last Minute Rent\"></div> <div class=\"campo\"><label>Reserva</label> <input type=\"text\" id=\"c-res\" value=\"R1\" placeholder=\"D014608185\"></div> </div> <div class=\"campo\"><label>Recogida</label> <input type=\"text\" id=\"c-rec\" value=\"\" placeholder=\"19 jul 15:00 · Aeropuerto\"></div> <div class=\"campo\"><label>Devolución</label> <input type=\"text\" id=\"c-dev\" value=\"\" placeholder=\"28 jul 15:00 · Aeropuerto\"></div> <div class=\"campo\"><label>Teléfono</label> <input type=\"text\" id=\"c-tel\" value=\"+34600000000\" placeholder=\"+39 342 013 8930\" inputmode=\"tel\"></div> </div> <div class=\"parada\"> <div class=\"fila\"> <input type=\"text\" class=\"mini t-q\" data-i=\"0\" value=\"Hotel\" placeholder=\"Quién es\"> <input type=\"text\" class=\"mini t-n\" data-i=\"0\" value=\"+34611111111\" placeholder=\"+34 600…\" inputmode=\"tel\"> <button class=\"btn fino borrar\" data-borrar-tel=\"0\">×</button> </div> </div></div> <div class=\"btns\"> <button class=\"btn\" id=\"add-vuelo\">+ Vuelo</button> <button class=\"btn\" id=\"add-tel\">+ Teléfono</button> </div> <p class=\"sec\">Guardar y compartir</p> <div id=\"aviso-nube\"><div class=\"aviso \"><span class=\"ic\">!</span><p>Sin conexión: se guarda aquí y subirá solo cuando haya cobertura.</p></div></div> <div class=\"btns\"> <button class=\"btn\" id=\"exportar\">Exportar viaje</button> <button class=\"btn\" id=\"importar\">Importar</button> <button class=\"btn borrar\" id=\"eliminar\">Eliminar viaje</button> </div> <div id=\"zona-exportar\"></div> <p class=\"nota\" id=\"estado\"></p> </main> <div class=\"barra\"> <button class=\"btn\" id=\"previsualizar\">Ver cómo queda</button> <button class=\"btn solid\" id=\"guardar\">Guardar</button> </div> <script> /* ═══════════════════════════════════════════════════════════ Sincronización de viajes con Supabase. Reglas de la casa: · El móvil manda. Todo se guarda primero en local y la app funciona igual aunque no haya cobertura ni sesión. · La nube es un espejo. Si hay red y sesión, se iguala. · Ante conflicto, gana la versión modificada más tarde. ═══════════════════════════════════════════════════════════ */ const SB_URL = \"https://cmkzcvfjgrgxwqjimtxa.supabase.co\"; const SB_KEY = \"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImNta3pjdmZqZ3JneHdxamltdHhhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODQ3NzU5NzAsImV4cCI6MjEwMDM1MTk3MH0.epSiwj0MO9WWfqETVoEt2E_ijNSzi4x0d-TmgDhAWhQ\"; const SB_CLAVE_VIAJES = \"viajes_propios\"; const SB_PENDIENTES = \"viajes_pendientes\"; /* Ninguna espera es infinita: si no responde, se avisa */ function conTope(promesa, ms, fallo){ return Promise.race([ promesa, new Promise(r => setTimeout(() => r(fallo), ms)) ]); } const SYNC = { cliente: null, sesion: null, /* ---- Local ---- */ locales(){ try { return JSON.parse(localStorage.getItem(SB_CLAVE_VIAJES)) || []; } catch { return []; } }, guardarLocales(v){ try { localStorage.setItem(SB_CLAVE_VIAJES, JSON.stringify(v)); return true; } catch { return false; } }, pendientes(){ try { return JSON.parse(localStorage.getItem(SB_PENDIENTES)) || []; } catch { return []; } }, marcarPendiente(id){ const p = new Set(this.pendientes()); p.add(id); try { localStorage.setItem(SB_PENDIENTES, JSON.stringify([...p])); } catch {} }, limpiarPendiente(id){ try { localStorage.setItem(SB_PENDIENTES, JSON.stringify(this.pendientes().filter(x => x !== id))); } catch {} }, /* ---- Conexión ---- */ async conectar(){ if (this.cliente) return this.cliente; if (!navigator.onLine) return null; try { if (!window.supabase){ const cargada = await new Promise(ok => { const s = document.createElement(\"script\"); s.src = \"https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/dist/umd/supabase.js\"; s.onload = () => ok(true); s.onerror = () => ok(false); document.head.appendChild(s); setTimeout(() => ok(false), 9000); }); if (!cargada || !window.supabase) return null; } this.cliente = window.supabase.createClient(SB_URL, SB_KEY); const r = await conTope(this.cliente.auth.getSession(), 8000, { data:null }); this.sesion = r?.data?.session || null; return this.cliente; } catch { this.cliente = null; return null; } }, /* ---- Estado, para pintarlo en pantalla ---- */ async estado(){ return conTope(this._estado(), 12000, { ok:false, txt:\"La nube no responde · solo en este móvil\" }); }, async _estado(){ if (!navigator.onLine) return { ok:false, txt:\"Sin conexión · solo en este móvil\" }; const c = await this.conectar(); if (!c) return { ok:false, txt:\"No se pudo conectar · solo en este móvil\" }; if (!this.sesion) return { ok:false, txt:\"Sin sesión\", login:true }; const p = this.pendientes().length; return { ok:true, correo: this.sesion.user?.email || \"\", txt: p ? `${p} viaje${p===1?\"\":\"s\"} por subir` : \"Sincronizado\", sesion:this.sesion }; }, /* ---- Entrar y salir ---- */ async entrar(correo, clave){ const c = await this.conectar(); if (!c) return { ok:false, txt:\"Sin conexión\" }; try { const r = await conTope( c.auth.signInWithPassword({ email:correo, password:clave }), 15000, { error:{ message:\"__tardando__\" } }); if (r.error) return { ok:false, txt: r.error.message === \"Invalid login credentials\" ? \"Correo o contraseña incorrectos\" : r.error.message === \"__tardando__\" ? \"El servidor no responde. Reinténtalo.\" : r.error.message }; this.sesion = r.data.session; return { ok:true }; } catch (e){ return { ok:false, txt:\"No se pudo entrar\" }; } }, async salir(){ const c = await this.conectar(); if (c) { try { await c.auth.signOut(); } catch {} } this.sesion = null; }, /* ---- Subir un viaje ---- */ async subir(viaje){ const c = await this.conectar(); if (!c || !this.sesion){ this.marcarPendiente(viaje.id); return false; } try { const { error } = await c.from(\"viajes\").upsert({ id: viaje.id, nombre: viaje.nombre, desde: viaje.desde || \"\", hasta: viaje.hasta || \"\", salida: viaje.salida || \"\", dias: viaje.dias || [], autor: this.sesion.user?.email || \"\", actualizado: new Date().toISOString(), borrado: false }); if (error) throw error; this.limpiarPendiente(viaje.id); return true; } catch { this.marcarPendiente(viaje.id); return false; } }, /* ---- Marcar como borrado (para que desaparezca del otro móvil) ---- */ async borrar(id){ const c = await this.conectar(); if (!c || !this.sesion) return false; try { await c.from(\"viajes\").update({ borrado:true, actualizado:new Date().toISOString() }).eq(\"id\", id); return true; } catch { return false; } }, /* ---- Bajar y fundir con lo local ---- */ async sincronizar(){ return conTope(this._sincronizar(), 25000, { cambios:0, ok:false }); }, async _sincronizar(){ const c = await this.conectar(); if (!c || !this.sesion) return { cambios:0, ok:false }; // primero, lo que quedó pendiente de subir const locales = this.locales(); for (const id of this.pendientes()){ const v = locales.find(x => x.id === id); if (v) await this.subir(v); else this.limpiarPendiente(id); } let remotos = []; try { const { data, error } = await c.from(\"viajes\").select(\"*\"); if (error) throw error; remotos = data || []; } catch { return { cambios:0, ok:false }; } const porId = new Map(locales.map(v => [v.id, v])); let cambios = 0; for (const r of remotos){ const l = porId.get(r.id); if (r.borrado){ if (l){ porId.delete(r.id); cambios++; } continue; } const nube = { id:r.id, nombre:r.nombre, desde:r.desde||\"\", hasta:r.hasta||\"\", salida:r.salida||\"\", dias:r.dias||[], actualizado:r.actualizado }; if (!l){ porId.set(r.id, nube); cambios++; continue; } // gana el más reciente const tl = Date.parse(l.actualizado || 0) || 0; const tr = Date.parse(r.actualizado || 0) || 0; if (tr > tl){ porId.set(r.id, nube); cambios++; } else if (tl > tr){ await this.subir(l); } } // lo que existe solo en el móvil, sube for (const l of locales){ if (!remotos.some(r => r.id === l.id)) await this.subir(l); } this.guardarLocales([...porId.values()]); return { cambios, ok:true }; } }; /* ═══════════════════════════════════════════════════════════ Diario compartido: paradas marcadas y notas de cada día. Cada entrada lleva su marca de tiempo, así que si los dos anotáis cosas distintas sin cobertura, al juntarse no se pierde ninguna: para cada parada gana el gesto más reciente. ═══════════════════════════════════════════════════════════ */ const DIARIO_SYNC = { clave(viaje){ return `diario_${viaje}`; }, local(viaje){ try { const d = JSON.parse(localStorage.getItem(this.clave(viaje))) || { hechas:{}, desmarcadas:{}, notas:{} }; d.posiciones = d.posiciones || {}; // dónde estabas al marcar: { \"5:0\": {xy, ts} } d.visitas = d.visitas || []; // «estoy aquí»: [{id, xy, ts, dia, txt}] d.pernoctas = d.pernoctas || []; // dónde dormimos: [{id, xy, ts, dia, txt, tipo, valorada}] d.portadas = d.portadas || {}; // foto de portada por día: { \"5\": {id, ts} } return d; } catch { return { hechas:{}, desmarcadas:{}, notas:{}, posiciones:{}, visitas:[], pernoctas:[], portadas:{} }; } }, guardarLocal(viaje, d){ try { localStorage.setItem(this.clave(viaje), JSON.stringify(d)); } catch {} }, /* ---- Gestos ---- */ marcar(viaje, parada, marcada){ const d = this.local(viaje), ahora = Date.now(); if (marcada){ d.hechas[parada] = ahora; delete d.desmarcadas[parada]; } else { d.desmarcadas[parada] = ahora; delete d.hechas[parada]; } this.guardarLocal(viaje, d); this.subir(viaje).catch(()=>{}); // y dónde estábamos: sin bloquear, y solo si el móvil lo da rápido if (marcada) this.apuntarDonde(viaje, parada); }, apuntarDonde(viaje, parada){ if (!navigator.geolocation) return; navigator.geolocation.getCurrentPosition(p => { const d = this.local(viaje); d.posiciones[parada] = { xy: `${p.coords.latitude.toFixed(5)},${p.coords.longitude.toFixed(5)}`, ts: Date.now() }; this.guardarLocal(viaje, d); this.subir(viaje).catch(()=>{}); }, () => {}, { enableHighAccuracy:false, timeout:8000, maximumAge:120000 }); }, /* Pregunta a OpenStreetMap cómo se llama este sitio */ async comoSeLlama(la, lo){ if (!navigator.onLine) return \"\"; const u = `https://nominatim.openstreetmap.org/reverse?format=jsonv2` + `&lat=${la}&lon=${lo}&zoom=16&accept-language=es`; const ctrl = new AbortController(); const corte = setTimeout(() => ctrl.abort(), 9000); try { const r = await fetch(u, { signal: ctrl.signal }); clearTimeout(corte); if (!r.ok) throw new Error(r.status); const d = await r.json(); const a = d.address || {}; // lo más concreto primero: el sitio, luego el pueblo, luego el municipio const sitio = d.name || a.tourism || a.amenity || a.natural || a.peak || \"\"; const pueblo = a.village || a.hamlet || a.town || a.city || a.suburb || a.locality || \"\"; const zona = a.municipality || a.county || \"\"; const partes = []; if (sitio) partes.push(sitio); if (pueblo && pueblo !== sitio) partes.push(pueblo); if (!partes.length && zona) partes.push(zona); return partes.join(\" · \"); } catch { clearTimeout(corte); return \"\"; } }, /* ---- «Estoy aquí»: marcar un sitio suelto, esté o no en el plan ---- */ apuntarAqui(viaje, dia, txt){ return new Promise((ok, mal) => { if (!navigator.geolocation) return mal(\"Este móvil no da la ubicación\"); navigator.geolocation.getCurrentPosition(async p => { const la = p.coords.latitude, lo = p.coords.longitude; const d = this.local(viaje); const v = { id: \"v\" + Date.now().toString(36), xy: `${la.toFixed(5)},${lo.toFixed(5)}`, ts: Date.now(), dia, txt: txt || \"\", precision: Math.round(p.coords.accuracy || 0) }; d.visitas.push(v); this.guardarLocal(viaje, d); ok(v); // se guarda ya, sin esperar al nombre // y por detrás, cómo se llama el sitio if (!txt){ const nombre = await this.comoSeLlama(la, lo); if (nombre){ const d2 = this.local(viaje); const v2 = (d2.visitas || []).find(x => x.id === v.id); if (v2 && !v2.txt){ v2.txt = nombre; v2.auto = true; this.guardarLocal(viaje, d2); } } } this.subir(viaje).catch(()=>{}); }, () => mal(\"No se pudo obtener la ubicación\"), { enableHighAccuracy:true, timeout:12000, maximumAge:20000 }); }); }, /* Buscar un sitio por su nombre, para añadirlo después de haber pasado */ async buscarSitio(texto, cerca){ if (!navigator.onLine) return []; let u = `https://nominatim.openstreetmap.org/search?format=jsonv2` + `&q=${encodeURIComponent(texto)}&limit=6&accept-language=es`; if (cerca){ // prioriza lo que esté cerca del viaje const [la, lo] = cerca.split(\",\").map(Number); u += `&viewbox=${lo-1.5},${la+1.5},${lo+1.5},${la-1.5}&bounded=0`; } const ctrl = new AbortController(); const corte = setTimeout(() => ctrl.abort(), 10000); try { const r = await fetch(u, { signal: ctrl.signal }); clearTimeout(corte); if (!r.ok) throw new Error(r.status); const d = await r.json(); return (d || []).map(x => ({ nombre: (x.name || x.display_name || \"\").split(\",\")[0].trim(), donde: (x.display_name || \"\").split(\",\").slice(1, 3).join(\",\").trim(), xy: `${Number(x.lat).toFixed(5)},${Number(x.lon).toFixed(5)}` })).filter(x => x.nombre); } catch { clearTimeout(corte); return []; } }, /* Añade un punto a un día ya pasado, colocándolo en su sitio del recorrido */ anadirVisita(viaje, dia, nombre, xy, fechaDia){ const d = this.local(viaje); // la hora se calcula para que quede en orden dentro de ese día const base = fechaDia ? new Date(fechaDia + \"T09:00:00\").getTime() : Date.now(); const cuantas = (d.visitas || []).filter(v => String(v.dia) === String(dia) && !v.borrada).length; const v = { id: \"v\" + Date.now().toString(36), xy, ts: base + cuantas * 20 * 60 * 1000, // veinte minutos entre punto y punto dia, txt: nombre, manual: true }; d.visitas.push(v); this.guardarLocal(viaje, d); this.subir(viaje).catch(()=>{}); return v; }, visitasDelDia(viaje, dia){ return (this.local(viaje).visitas || []) .filter(v => String(v.dia) === String(dia) && !v.borrada) .sort((a,b) => a.ts - b.ts); }, renombrarVisita(viaje, id, txt){ const d = this.local(viaje); const v = (d.visitas || []).find(x => x.id === id); if (!v) return; v.txt = txt; v.ts_txt = Date.now(); this.guardarLocal(viaje, d); this.subir(viaje).catch(()=>{}); }, borrarVisita(viaje, id){ const d = this.local(viaje); const v = (d.visitas || []).find(x => x.id === id); if (!v) return; v.borrada = true; v.ts_txt = Date.now(); this.guardarLocal(viaje, d); this.subir(viaje).catch(()=>{}); }, /* ---- Dónde dormimos: para volver, o para no volver ---- */ async apuntarPernocta(viaje, dia, datos = {}){ return new Promise((ok, mal) => { if (!navigator.geolocation) return mal(\"Este móvil no da la ubicación\"); navigator.geolocation.getCurrentPosition(async p => { const la = p.coords.latitude, lo = p.coords.longitude; const d = this.local(viaje); d.pernoctas = d.pernoctas || []; const n = { id: \"p\" + Date.now().toString(36), xy: `${la.toFixed(5)},${lo.toFixed(5)}`, ts: Date.now(), dia, txt: datos.txt || \"\", tipo: datos.tipo || \"\", nota: datos.nota || \"\", valorada: datos.valorada || 0 // 0 sin valorar, 1 mal, 2 normal, 3 repetiría }; d.pernoctas.push(n); this.guardarLocal(viaje, d); ok(n); if (!n.txt){ const nombre = await this.comoSeLlama(la, lo); if (nombre){ const d2 = this.local(viaje); const n2 = (d2.pernoctas || []).find(x => x.id === n.id); if (n2 && !n2.txt){ n2.txt = nombre; n2.auto = true; this.guardarLocal(viaje, d2); } } } this.subir(viaje).catch(()=>{}); }, () => mal(\"No se pudo obtener la ubicación\"), { enableHighAccuracy:true, timeout:12000, maximumAge:20000 }); }); }, pernoctaDelDia(viaje, dia){ return (this.local(viaje).pernoctas || []) .filter(x => String(x.dia) === String(dia) && !x.borrada) .sort((a,b) => b.ts - a.ts)[0] || null; }, todasLasPernoctas(viaje){ return (this.local(viaje).pernoctas || []) .filter(x => !x.borrada).sort((a,b) => a.ts - b.ts); }, editarPernocta(viaje, id, cambios){ const d = this.local(viaje); const n = (d.pernoctas || []).find(x => x.id === id); if (!n) return; Object.assign(n, cambios, { ts_edit: Date.now() }); this.guardarLocal(viaje, d); this.subir(viaje).catch(()=>{}); }, borrarPernocta(viaje, id){ this.editarPernocta(viaje, id, { borrada: true }); }, /* ---- Foto de portada del día ---- */ portada(viaje, dia){ return this.local(viaje).portadas?.[dia]?.id || null; }, ponPortada(viaje, dia, id){ const d = this.local(viaje); if (d.portadas[dia]?.id === id) delete d.portadas[dia]; else d.portadas[dia] = { id, ts: Date.now() }; this.guardarLocal(viaje, d); this.subir(viaje).catch(()=>{}); }, /* Los sitios donde estuvisteis de verdad, en orden */ recorridoReal(viaje){ const d = this.local(viaje); const deMarcas = Object.entries(d.posiciones || {}) .map(([clave, v]) => ({ dia: +clave.split(\":\")[0], xy: v.xy, ts: v.ts, tipo: \"marca\" })); const deVisitas = (d.visitas || []).filter(v => !v.borrada) .map(v => ({ dia: v.dia, xy: v.xy, ts: v.ts, txt: v.txt, tipo: \"visita\" })); return [...deMarcas, ...deVisitas].filter(x => x.xy).sort((a, b) => a.ts - b.ts); }, anotar(viaje, dia, texto){ const d = this.local(viaje); d.notas[dia] = { t: texto, ts: Date.now() }; this.guardarLocal(viaje, d); clearTimeout(this._espera); this._espera = setTimeout(() => this.subir(viaje).catch(()=>{}), 2500); }, /* ---- Consultas ---- */ estaHecha(viaje, parada){ return !!this.local(viaje).hechas[parada]; }, nota(viaje, dia){ return this.local(viaje).notas[dia]?.t || \"\"; }, /* ---- Fundir dos versiones sin perder nada ---- */ fundir(a, b){ const r = { hechas:{}, desmarcadas:{}, notas:{} }; const claves = new Set([ ...Object.keys(a.hechas||{}), ...Object.keys(a.desmarcadas||{}), ...Object.keys(b.hechas||{}), ...Object.keys(b.desmarcadas||{}) ]); for (const k of claves){ const marcada = Math.max(a.hechas?.[k] || 0, b.hechas?.[k] || 0); const desmarcada = Math.max(a.desmarcadas?.[k] || 0, b.desmarcadas?.[k] || 0); if (marcada === 0 && desmarcada === 0) continue; if (marcada >= desmarcada) r.hechas[k] = marcada; else r.desmarcadas[k] = desmarcada; } const dias = new Set([...Object.keys(a.notas||{}), ...Object.keys(b.notas||{})]); for (const k of dias){ const na = a.notas?.[k], nb = b.notas?.[k]; if (!na) { r.notas[k] = nb; continue; } if (!nb) { r.notas[k] = na; continue; } r.notas[k] = (nb.ts || 0) > (na.ts || 0) ? nb : na; } // las visitas se acumulan: cada una tiene su id, gana la versión más retocada const porId = new Map(); for (const v of [...(a.visitas||[]), ...(b.visitas||[])]){ const antes = porId.get(v.id); if (!antes || (v.ts_txt || v.ts || 0) > (antes.ts_txt || antes.ts || 0)) porId.set(v.id, v); } r.visitas = [...porId.values()].sort((x,y) => x.ts - y.ts); const pn = new Map(); for (const x of [...(a.pernoctas||[]), ...(b.pernoctas||[])]){ const antes = pn.get(x.id); if (!antes || (x.ts_edit || x.ts || 0) > (antes.ts_edit || antes.ts || 0)) pn.set(x.id, x); } r.pernoctas = [...pn.values()].sort((x,y) => x.ts - y.ts); r.portadas = {}; const dp = new Set([...Object.keys(a.portadas||{}), ...Object.keys(b.portadas||{})]); for (const k of dp){ const pa = a.portadas?.[k], pb = b.portadas?.[k]; if (!pa) { r.portadas[k] = pb; continue; } if (!pb) { r.portadas[k] = pa; continue; } r.portadas[k] = (pb.ts || 0) > (pa.ts || 0) ? pb : pa; } r.posiciones = {}; const sitios = new Set([...Object.keys(a.posiciones||{}), ...Object.keys(b.posiciones||{})]); for (const k of sitios){ const pa = a.posiciones?.[k], pb = b.posiciones?.[k]; if (!pa) { r.posiciones[k] = pb; continue; } if (!pb) { r.posiciones[k] = pa; continue; } r.posiciones[k] = (pb.ts || 0) > (pa.ts || 0) ? pb : pa; // el primero que llegó manda } return r; }, async subir(viaje){ const c = await SYNC.conectar(); if (!c || !SYNC.sesion) return false; const d = this.local(viaje); try { const { error } = await c.from(\"viaje_diario\").upsert({ viaje, hechas:d.hechas, desmarcadas:d.desmarcadas, notas:d.notas, posiciones:d.posiciones, visitas:d.visitas, pernoctas:d.pernoctas, portadas:d.portadas, actualizado: new Date().toISOString() }); return !error; } catch { return false; } }, /* Baja lo del otro móvil, lo funde y devuelve si algo cambió */ async sincronizar(viaje){ const c = await SYNC.conectar(); if (!c || !SYNC.sesion) return { ok:false, cambios:false }; let remoto = null; try { const { data, error } = await c.from(\"viaje_diario\") .select(\"*\").eq(\"viaje\", viaje).maybeSingle(); if (error) throw error; remoto = data; } catch { return { ok:false, cambios:false }; } const antes = this.local(viaje); if (!remoto){ await this.subir(viaje); return { ok:true, cambios:false }; } const fundido = this.fundir(antes, { hechas: remoto.hechas || {}, desmarcadas: remoto.desmarcadas || {}, notas: remoto.notas || {}, posiciones: remoto.posiciones || {}, visitas: remoto.visitas || [], pernoctas: remoto.pernoctas || [], portadas: remoto.portadas || {} }); const cambios = JSON.stringify(fundido) !== JSON.stringify(antes); this.guardarLocal(viaje, fundido); if (cambios) await this.subir(viaje); return { ok:true, cambios }; } }; /* ═══════════════════════════════════════════════════════════ Fotos del viaje. Van en IndexedDB, no en localStorage: allí solo caben unos 5 MB en total y tres fotos ya lo llenarían. Aquí caben cientos. Se guardan en el móvil. No se sincronizan todavía: una foto comprimida son 100 KB y meterlas en la fila del viaje sería pesado. Para compartirlas, de momento, el compartir del móvil. ═══════════════════════════════════════════════════════════ */ const FOTOS = { _bd: null, async abrir(){ if (this._bd) return this._bd; if (!window.indexedDB) return null; return new Promise(ok => { const p = indexedDB.open(\"viajes_fotos\", 1); p.onupgradeneeded = () => { const bd = p.result; if (!bd.objectStoreNames.contains(\"fotos\")) bd.createObjectStore(\"fotos\", { keyPath: \"id\" }); }; p.onsuccess = () => { this._bd = p.result; ok(this._bd); }; p.onerror = () => ok(null); setTimeout(() => ok(null), 5000); }); }, async _tienda(modo){ const bd = await this.abrir(); if (!bd) return null; try { return bd.transaction(\"fotos\", modo).objectStore(\"fotos\"); } catch { return null; } }, /* Guarda una foto y devuelve su id */ async guardar(viaje, dia, datos, xy){ const t = await this._tienda(\"readwrite\"); if (!t) return null; const id = `${viaje}:${dia}:${Date.now()}`; const guardado = await new Promise(ok => { const p = t.put({ id, viaje, dia, datos, xy: xy || null, cuando: Date.now() }); p.onsuccess = () => ok(id); p.onerror = () => ok(null); }); if (guardado) this.subir(id).catch(()=>{}); return guardado; }, /* Todas las de un día, de la más antigua a la más nueva */ async delDia(viaje, dia){ const t = await this._tienda(\"readonly\"); if (!t) return []; return new Promise(ok => { const fuera = []; const p = t.openCursor(); p.onsuccess = e => { const c = e.target.result; if (!c) return ok(fuera.sort((a,b) => a.cuando - b.cuando)); if (c.value.viaje === viaje && String(c.value.dia) === String(dia)) fuera.push(c.value); c.continue(); }; p.onerror = () => ok([]); }); }, /* Cuántas hay por día, para pintar las miniaturas de la lista */ async cuentaPorDia(viaje){ const t = await this._tienda(\"readonly\"); if (!t) return {}; return new Promise(ok => { const n = {}; const p = t.openCursor(); p.onsuccess = e => { const c = e.target.result; if (!c) return ok(n); if (c.value.viaje === viaje) n[c.value.dia] = (n[c.value.dia] || 0) + 1; c.continue(); }; p.onerror = () => ok({}); }); }, async una(id){ return this._porId(id); }, async borrar(id){ const t = await this._tienda(\"readwrite\"); if (!t) return false; const fuera = await new Promise(ok => { const p = t.delete(id); p.onsuccess = () => ok(true); p.onerror = () => ok(false); }); this.marcarBorradaFuera(id).catch(()=>{}); return fuera; }, /* ---- Compartir con el otro móvil ---- */ pendientes(){ try { return JSON.parse(localStorage.getItem(\"fotos_pendientes\")) || []; } catch { return []; } }, _apuntarPendiente(id){ const p = new Set(this.pendientes()); p.add(id); try { localStorage.setItem(\"fotos_pendientes\", JSON.stringify([...p])); } catch {} }, _quitarPendiente(id){ try { localStorage.setItem(\"fotos_pendientes\", JSON.stringify(this.pendientes().filter(x => x !== id))); } catch {} }, async _porId(id){ const t = await this._tienda(\"readonly\"); if (!t) return null; return new Promise(ok => { const p = t.get(id); p.onsuccess = () => ok(p.result || null); p.onerror = () => ok(null); }); }, async subir(id){ const c = await SYNC.conectar(); if (!c || !SYNC.sesion){ this._apuntarPendiente(id); return false; } const f = await this._porId(id); if (!f){ this._quitarPendiente(id); return false; } try { const { error } = await c.from(\"viaje_fotos\").upsert({ id: f.id, viaje: f.viaje, dia: f.dia, datos: f.datos, autor: SYNC.sesion.user?.email || \"\", borrada: false }); if (error) throw error; this._quitarPendiente(id); return true; } catch { this._apuntarPendiente(id); return false; } }, async marcarBorradaFuera(id){ const c = await SYNC.conectar(); if (!c || !SYNC.sesion) return false; try { await c.from(\"viaje_fotos\").update({ borrada:true }).eq(\"id\", id); this._quitarPendiente(id); return true; } catch { return false; } }, /* Trae del otro móvil las fotos de un día que aquí no estén. Primero pregunta qué ids hay (ligero) y solo baja las que faltan. */ async traerDelDia(viaje, dia){ const c = await SYNC.conectar(); if (!c || !SYNC.sesion) return 0; // lo pendiente de subir, primero for (const id of this.pendientes()) await this.subir(id); let fuera = []; try { const { data, error } = await c.from(\"viaje_fotos\") .select(\"id,borrada\").eq(\"viaje\", viaje).eq(\"dia\", dia); if (error) throw error; fuera = data || []; } catch { return 0; } const aqui = new Set((await this.delDia(viaje, dia)).map(f => f.id)); let nuevas = 0; for (const f of fuera){ if (f.borrada){ if (aqui.has(f.id)){ const t = await this._tienda(\"readwrite\"); if (t) t.delete(f.id); nuevas++; } continue; } if (aqui.has(f.id)) continue; try { const { data, error } = await c.from(\"viaje_fotos\") .select(\"id,viaje,dia,datos,cuando\").eq(\"id\", f.id).maybeSingle(); if (error || !data) continue; const t = await this._tienda(\"readwrite\"); if (!t) continue; t.put({ id:data.id, viaje:data.viaje, dia:data.dia, datos:data.datos, cuando: Date.parse(data.cuando) || Date.now() }); nuevas++; } catch {} } return nuevas; }, /* Cuánto ocupan, para avisar antes de llenar el móvil */ async espacio(){ try { if (!navigator.storage?.estimate) return null; const e = await navigator.storage.estimate(); return { usado: e.usage || 0, total: e.quota || 0 }; } catch { return null; } } }; /* Comprime antes de guardar: una foto de iPhone son 4 MB, esto la deja en ~120 KB */ function comprimirFoto(archivo, maxLado = 1400, calidad = 0.75){ return new Promise((ok, mal) => { const lector = new FileReader(); lector.onload = () => { const img = new Image(); img.onload = () => { let { width:a, height:b } = img; const f = Math.min(1, maxLado / Math.max(a, b)); a = Math.round(a * f); b = Math.round(b * f); const c = document.createElement(\"canvas\"); c.width = a; c.height = b; c.getContext(\"2d\").drawImage(img, 0, 0, a, b); ok(c.toDataURL(\"image/jpeg\", calidad)); }; img.onerror = mal; img.src = lector.result; }; lector.onerror = mal; lector.readAsDataURL(archivo); }); } // Reintento automático al recuperar la cobertura window.addEventListener(\"online\", () => { SYNC.sincronizar().catch(()=>{}); FOTOS.pendientes().forEach(id => FOTOS.subir(id).catch(()=>{})); }); </script> <script> /* ---- Modo de color, compartido por todas las apps ---- */ const TEMA = { get valor(){ try { return localStorage.getItem(\"tema_viajes\") || \"auto\"; } catch { return \"auto\"; } }, set valor(v){ try { localStorage.setItem(\"tema_viajes\", v); } catch {}; this.aplica(); }, pintaMini(){ const z = document.getElementById(\"tema-zona\"); if (!z) return; z.innerHTML = this.mini(); z.querySelectorAll(\"[data-tema]\").forEach(b => b.addEventListener(\"click\", () => { TEMA.valor = b.dataset.tema; TEMA.pintaMini(); })); }, aplica(){ const v = this.valor; document.body.classList.toggle(\"claro\", v === \"claro\"); document.body.classList.toggle(\"auto\", v === \"auto\"); const m = document.querySelector('meta[name=\"theme-color\"]'); if (!m) return; const oscuro = m.dataset.oscuro || m.getAttribute(\"content\"); if (!m.dataset.oscuro) m.dataset.oscuro = oscuro; const claroDeVerdad = v === \"claro\" || (v === \"auto\" && window.matchMedia && matchMedia(\"(prefers-color-scheme: light)\").matches); m.setAttribute(\"content\", claroDeVerdad ? \"#F4F6F7\" : m.dataset.oscuro); }, mini(){ const v = this.valor; const b = (id, t, etiqueta) => `<button class=\"tm${v === id ? \" on\" : \"\"}\" data-tema=\"${id}\" aria-label=\"${etiqueta}\" title=\"${etiqueta}\">${t}</button>`; return `<div class=\"tema-mini\">${b(\"auto\",\"◐\",\"Automático\")}${b(\"claro\",\"☀\",\"Claro\")}${b(\"oscuro\",\"☾\",\"Oscuro\")}</div>`; }, opciones(){ const v = this.valor; const b = (id, t, sub) => `<button class=\"op${v === id ? \" on\" : \"\"}\" data-tema=\"${id}\"><b>${t}</b><small>${sub}</small></button>`; return `<div class=\"ops\">${b(\"auto\",\"Automático\",\"Como el móvil\")}${b(\"claro\",\"Claro\",\"Para el sol\")}${b(\"oscuro\",\"Oscuro\",\"Para la noche\")}</div>`; }, engancha(volver){ document.querySelectorAll(\"[data-tema]\").forEach(x => x.addEventListener(\"click\", () => { TEMA.valor = x.dataset.tema; volver && volver(); })); } }; if (window.matchMedia) matchMedia(\"(prefers-color-scheme: light)\").addEventListener?.(\"change\", () => TEMA.aplica()); const CLAVE = \"viajes_propios\"; const esc = s => String(s??\"\").replace(/[&<>\"]/g, c => ({\"&\":\"&amp;\",\"<\":\"&lt;\",\">\":\"&gt;\",'\"':\"&quot;\"}[c])); const nuevoId = () => \"v\" + Date.now().toString(36); function leerTodos(){ try { return JSON.parse(localStorage.getItem(CLAVE)) || []; } catch { return []; } } function guardarTodos(v){ try { localStorage.setItem(CLAVE, JSON.stringify(v)); return true; } catch { return false; } } /* ---- Viaje en edición ---- */ const params = new URLSearchParams(location.search); let viaje = null; const idExistente = params.get(\"id\"); if (idExistente) viaje = leerTodos().find(v => v.id === idExistente) || null; if (!viaje) viaje = { id:nuevoId(), nombre:\"\", desde:\"\", hasta:\"\", salida:\"\", dias:[] }; if (idExistente && viaje.nombre){ document.getElementById(\"titulo\").textContent = viaje.nombre; document.getElementById(\"sub\").textContent = \"Editando un viaje que ya tenías.\"; } document.getElementById(\"nombre\").value = viaje.nombre; document.getElementById(\"desde\").value = viaje.desde; document.getElementById(\"hasta\").value = viaje.hasta; document.getElementById(\"salida\").value = viaje.salida; /* ---- Días y paradas ---- */ function pintaReservas(){ viaje.reservas = viaje.reservas || { vuelos:[], coche:{}, telefonos:[] }; const r = viaje.reservas; const c = r.coche || (r.coche = {}); document.getElementById(\"reservas\").innerHTML = r.vuelos.map((v, i) => ` <div class=\"dia\"> <div class=\"dia-cab\"> <span class=\"dia-num\">✈</span> <h2>${esc(v.ruta) || \"Vuelo\"}</h2> <button class=\"btn fino borrar\" data-borrar-vuelo=\"${i}\">Quitar</button> </div> <div class=\"campo\"><label>Trayecto</label> <input type=\"text\" class=\"v-ruta\" data-i=\"${i}\" value=\"${esc(v.ruta)}\" placeholder=\"Madrid MAD → Zagreb ZAG\"></div> <div class=\"dos\"> <div class=\"campo\"><label>Fecha</label> <input type=\"text\" class=\"v-fecha\" data-i=\"${i}\" value=\"${esc(v.fecha)}\" placeholder=\"18 jul\"></div> <div class=\"campo\"><label>Hora</label> <input type=\"text\" class=\"v-hora\" data-i=\"${i}\" value=\"${esc(v.hora)}\" placeholder=\"18:55\"></div> </div> <div class=\"dos\"> <div class=\"campo\"><label>Compañía</label> <input type=\"text\" class=\"v-cia\" data-i=\"${i}\" value=\"${esc(v.cia)}\" placeholder=\"Vueling\"></div> <div class=\"campo\"><label>Localizador</label> <input type=\"text\" class=\"v-loc\" data-i=\"${i}\" value=\"${esc(v.loc)}\" placeholder=\"ABC123\" autocapitalize=\"characters\"></div> </div> <div class=\"campo\"><label>Notas</label> <input type=\"text\" class=\"v-n\" data-i=\"${i}\" value=\"${esc(v.n)}\" placeholder=\"Terminal 4, facturar equipaje…\"></div> </div>`).join(\"\") + `<div class=\"dia\"> <div class=\"dia-cab\"><span class=\"dia-num\">🚗</span><h2>Coche de alquiler</h2></div> <div class=\"dos\"> <div class=\"campo\"><label>Empresa</label> <input type=\"text\" id=\"c-emp\" value=\"${esc(c.empresa)}\" placeholder=\"Last Minute Rent\"></div> <div class=\"campo\"><label>Reserva</label> <input type=\"text\" id=\"c-res\" value=\"${esc(c.reserva)}\" placeholder=\"D014608185\"></div> </div> <div class=\"campo\"><label>Recogida</label> <input type=\"text\" id=\"c-rec\" value=\"${esc(c.recogida)}\" placeholder=\"19 jul 15:00 · Aeropuerto\"></div> <div class=\"campo\"><label>Devolución</label> <input type=\"text\" id=\"c-dev\" value=\"${esc(c.devolucion)}\" placeholder=\"28 jul 15:00 · Aeropuerto\"></div> <div class=\"campo\"><label>Teléfono</label> <input type=\"text\" id=\"c-tel\" value=\"${esc(c.telefono)}\" placeholder=\"+39 342 013 8930\" inputmode=\"tel\"></div> </div>` + r.telefonos.map((t, i) => ` <div class=\"parada\"> <div class=\"fila\"> <input type=\"text\" class=\"mini t-q\" data-i=\"${i}\" value=\"${esc(t.q)}\" placeholder=\"Quién es\"> <input type=\"text\" class=\"mini t-n\" data-i=\"${i}\" value=\"${esc(t.n)}\" placeholder=\"+34 600…\" inputmode=\"tel\"> <button class=\"btn fino borrar\" data-borrar-tel=\"${i}\">×</button> </div> </div>`).join(\"\"); const liga = (sel, obj, campo) => document.querySelectorAll(sel).forEach(el => el.addEventListener(\"input\", () => { obj(el)[campo] = el.value; sucio(); })); liga(\".v-ruta\", el => r.vuelos[+el.dataset.i], \"ruta\"); liga(\".v-fecha\", el => r.vuelos[+el.dataset.i], \"fecha\"); liga(\".v-hora\", el => r.vuelos[+el.dataset.i], \"hora\"); liga(\".v-cia\", el => r.vuelos[+el.dataset.i], \"cia\"); liga(\".v-loc\", el => r.vuelos[+el.dataset.i], \"loc\"); liga(\".v-n\", el => r.vuelos[+el.dataset.i], \"n\"); liga(\".t-q\", el => r.telefonos[+el.dataset.i], \"q\"); liga(\".t-n\", el => r.telefonos[+el.dataset.i], \"n\"); [[\"c-emp\",\"empresa\"],[\"c-res\",\"reserva\"],[\"c-rec\",\"recogida\"], [\"c-dev\",\"devolucion\"],[\"c-tel\",\"telefono\"]].forEach(([id, campo]) => { const el = document.getElementById(id); if (el) el.addEventListener(\"input\", () => { c[campo] = el.value; sucio(); }); }); document.querySelectorAll(\"[data-borrar-vuelo]\").forEach(b => b.addEventListener(\"click\", () => { r.vuelos.splice(+b.dataset.borrarVuelo, 1); pintaReservas(); sucio(); })); document.querySelectorAll(\"[data-borrar-tel]\").forEach(b => b.addEventListener(\"click\", () => { r.telefonos.splice(+b.dataset.borrarTel, 1); pintaReservas(); sucio(); })); document.querySelectorAll(\".v-ruta\").forEach(el => el.addEventListener(\"input\", () => { el.closest(\".dia\").querySelector(\"h2\").textContent = el.value || \"Vuelo\"; })); } document.getElementById(\"add-vuelo\").addEventListener(\"click\", () => { viaje.reservas = viaje.reservas || { vuelos:[], coche:{}, telefonos:[] }; viaje.reservas.vuelos.push({ruta:\"\",fecha:\"\",hora:\"\",cia:\"\",loc:\"\",n:\"\"}); pintaReservas(); sucio(); }); document.getElementById(\"add-tel\").addEventListener(\"click\", () => { viaje.reservas = viaje.reservas || { vuelos:[], coche:{}, telefonos:[] }; viaje.reservas.telefonos.push({q:\"\",n:\"\"}); pintaReservas(); sucio(); }); function pintaDias(){ const cont = document.getElementById(\"dias\"); if (!viaje.dias.length){ cont.innerHTML = `<p class=\"vacio\">Todavía no hay ningún día. Añade el primero.</p>`; return; } cont.innerHTML = viaje.dias.map((d, i) => ` <div class=\"dia\" data-dia=\"${i}\"> <div class=\"dia-cab\"> <span class=\"dia-num\">${i+1}</span> <h2>${esc(d.t) || \"Sin título\"}</h2> <button class=\"btn fino borrar\" data-borrar-dia=\"${i}\">Quitar</button> </div> <div class=\"campo\"> <label>Qué se hace ese día</label> <input type=\"text\" class=\"d-t\" data-i=\"${i}\" value=\"${esc(d.t)}\" placeholder=\"Subida a Somiedo\"> </div> <div class=\"campo\"> <label>Dónde se duerme</label> <input type=\"text\" class=\"d-dest\" data-i=\"${i}\" value=\"${esc(d.dest)}\" placeholder=\"Pola de Somiedo\"> </div> <label style=\"font-family:var(--mono);font-size:10.5px;letter-spacing:.18em; text-transform:uppercase;color:var(--tinta-dim);display:block;margin:14px 0 7px\">Paradas</label> ${d.paradas.map((p, k) => ` <div class=\"parada\"> <div class=\"fila\"> <input type=\"text\" class=\"mini p-h\" data-i=\"${i}\" data-k=\"${k}\" value=\"${esc(p.h)}\" placeholder=\"Mañana\"> <input type=\"text\" class=\"mini p-txt\" data-i=\"${i}\" data-k=\"${k}\" value=\"${esc(p.txt)}\" placeholder=\"Qué se visita\"> <button class=\"btn fino borrar\" data-borrar-par=\"${i}:${k}\">×</button> </div> <input type=\"text\" class=\"mini p-n\" data-i=\"${i}\" data-k=\"${k}\" value=\"${esc(p.n)}\" placeholder=\"Nota: horarios, avisos…\" style=\"margin-top:7px\"> <input type=\"text\" class=\"mini p-mapa\" data-i=\"${i}\" data-k=\"${k}\" value=\"${esc(p.mapa)}\" placeholder=\"Nombre para el navegador\" style=\"margin-top:7px\"> </div>`).join(\"\")} <div class=\"btns\"> <button class=\"btn fino\" data-add-par=\"${i}\">+ Parada</button> <button class=\"btn fino busca\" data-buscar=\"${i}\">Buscar un sitio</button> </div> <div class=\"buscador-zona\" id=\"bz-${i}\"></div> <label style=\"font-family:var(--mono);font-size:10.5px;letter-spacing:.18em; text-transform:uppercase;color:var(--tinta-dim);display:block;margin:16px 0 7px\">Foto del día</label> ${d.foto ? `<div class=\"foto-zona\"> <img src=\"${d.foto}\" alt=\"\"> <button class=\"btn fino borrar\" data-quitar-foto=\"${i}\">Quitar foto</button> </div>` : `<label class=\"subir\"> <input type=\"file\" accept=\"image/*\" data-foto=\"${i}\" hidden> <span>+ Añadir una foto</span> </label>`} </div>`).join(\"\"); engancha(); enganchaFotos(); document.querySelectorAll(\"[data-buscar]\").forEach(b => { if (b.dataset.l) return; b.dataset.l = 1; b.addEventListener(\"click\", () => abreBuscador(+b.dataset.buscar)); }); } function engancha(){ const set = (sel, campo) => document.querySelectorAll(sel).forEach(el => el.addEventListener(\"input\", () => { viaje.dias[+el.dataset.i][campo] = el.value; sucio(); })); set(\".d-t\", \"t\"); set(\".d-dest\", \"dest\"); const setP = (sel, campo) => document.querySelectorAll(sel).forEach(el => el.addEventListener(\"input\", () => { viaje.dias[+el.dataset.i].paradas[+el.dataset.k][campo] = el.value; sucio(); })); setP(\".p-h\",\"h\"); setP(\".p-txt\",\"txt\"); setP(\".p-n\",\"n\"); setP(\".p-mapa\",\"mapa\"); document.querySelectorAll(\".d-t\").forEach(el => el.addEventListener(\"input\", () => { const h = el.closest(\".dia\").querySelector(\"h2\"); h.textContent = el.value || \"Sin título\"; })); document.querySelectorAll(\"[data-add-par]\").forEach(b => b.addEventListener(\"click\", () => { viaje.dias[+b.dataset.addPar].paradas.push({h:\"\", txt:\"\", n:\"\", mapa:\"\"}); async function pintaAvisoNube(){ const z = document.getElementById(\"aviso-nube\"); if (!z) return; const pinta = (clase, icono, texto) => z.innerHTML = `<div class=\"aviso ${clase}\"><span class=\"ic\">${icono}</span><p>${texto}</p></div>`; pinta(\"\", \"…\", \"Comprobando si se puede sincronizar…\"); if (typeof SYNC === \"undefined\") return pinta(\"\", \"!\", \"El viaje se guarda en este móvil. Para que lo vea otra persona, expórtalo.\"); try { const e = await SYNC.estado(); if (e.ok) pinta(\"ok\", \"✓\", `Se guarda aquí y se sube a la nube. Pilar lo verá en su móvil en cuanto abra la app, sin hacer nada.`); else if (e.login) pinta(\"\", \"!\", `Sin sesión: por ahora solo se guarda en este móvil. Entra desde <a href=\"../\">Viajes</a> para que lo vea Pilar.`); else pinta(\"\", \"!\", `Sin conexión: se guarda aquí y subirá solo cuando haya cobertura.`); } catch { pinta(\"\", \"!\", \"El viaje se guarda en este móvil.\"); } } sucio(); })); document.querySelectorAll(\"[data-borrar-par]\").forEach(b => b.addEventListener(\"click\", () => { const [i,k] = b.dataset.borrarPar.split(\":\").map(Number); viaje.dias[i].paradas.splice(k,1); async function pintaAvisoNube(){ const z = document.getElementById(\"aviso-nube\"); if (!z) return; const pinta = (clase, icono, texto) => z.innerHTML = `<div class=\"aviso ${clase}\"><span class=\"ic\">${icono}</span><p>${texto}</p></div>`; pinta(\"\", \"…\", \"Comprobando si se puede sincronizar…\"); if (typeof SYNC === \"undefined\") return pinta(\"\", \"!\", \"El viaje se guarda en este móvil. Para que lo vea otra persona, expórtalo.\"); try { const e = await SYNC.estado(); if (e.ok) pinta(\"ok\", \"✓\", `Se guarda aquí y se sube a la nube. Pilar lo verá en su móvil en cuanto abra la app, sin hacer nada.`); else if (e.login) pinta(\"\", \"!\", `Sin sesión: por ahora solo se guarda en este móvil. Entra desde <a href=\"../\">Viajes</a> para que lo vea Pilar.`); else pinta(\"\", \"!\", `Sin conexión: se guarda aquí y subirá solo cuando haya cobertura.`); } catch { pinta(\"\", \"!\", \"El viaje se guarda en este móvil.\"); } } pintaDias(); pintaReservas(); setTimeout(pintaAvisoNube, 300); sucio(); })); document.querySelectorAll(\"[data-borrar-dia]\").forEach(b => b.addEventListener(\"click\", () => { if (!confirm(\"¿Quitar este día entero?\")) return; viaje.dias.splice(+b.dataset.borrarDia,1); async function pintaAvisoNube(){ const z = document.getElementById(\"aviso-nube\"); if (!z) return; const pinta = (clase, icono, texto) => z.innerHTML = `<div class=\"aviso ${clase}\"><span class=\"ic\">${icono}</span><p>${texto}</p></div>`; pinta(\"\", \"…\", \"Comprobando si se puede sincronizar…\"); if (typeof SYNC === \"undefined\") return pinta(\"\", \"!\", \"El viaje se guarda en este móvil. Para que lo vea otra persona, expórtalo.\"); try { const e = await SYNC.estado(); if (e.ok) pinta(\"ok\", \"✓\", `Se guarda aquí y se sube a la nube. Pilar lo verá en su móvil en cuanto abra la app, sin hacer nada.`); else if (e.login) pinta(\"\", \"!\", `Sin sesión: por ahora solo se guarda en este móvil. Entra desde <a href=\"../\">Viajes</a> para que lo vea Pilar.`); else pinta(\"\", \"!\", `Sin conexión: se guarda aquí y subirá solo cuando haya cobertura.`); } catch { pinta(\"\", \"!\", \"El viaje se guarda en este móvil.\"); } } pintaDias(); pintaReservas(); setTimeout(pintaAvisoNube, 300); sucio(); })); } /* ---- Fotos: se comprimen antes de guardar ---- */ function comprimir(archivo, maxLado = 900, calidad = 0.72){ return new Promise((ok, mal) => { const lector = new FileReader(); lector.onload = () => { const img = new Image(); img.onload = () => { let { width:a, height:b } = img; const f = Math.min(1, maxLado / Math.max(a, b)); a = Math.round(a * f); b = Math.round(b * f); const c = document.createElement(\"canvas\"); c.width = a; c.height = b; c.getContext(\"2d\").drawImage(img, 0, 0, a, b); ok(c.toDataURL(\"image/jpeg\", calidad)); }; img.onerror = mal; img.src = lector.result; }; lector.onerror = mal; lector.readAsDataURL(archivo); }); } function enganchaFotos(){ document.querySelectorAll(\"[data-foto]\").forEach(inp => { if (inp.dataset.l) return; inp.dataset.l = 1; inp.addEventListener(\"change\", async e => { const f = e.target.files?.[0]; if (!f) return; const eti = inp.closest(\".subir\").querySelector(\"span\"); eti.textContent = \"Preparando la foto…\"; try { const datos = await comprimir(f); const kb = Math.round(datos.length * 0.75 / 1024); if (kb > 600){ eti.textContent = \"+ Añadir una foto\"; alert(\"Esa foto pesa demasiado incluso comprimida. Prueba con otra.\"); return; } viaje.dias[+inp.dataset.foto].foto = datos; async function pintaAvisoNube(){ const z = document.getElementById(\"aviso-nube\"); if (!z) return; const pinta = (clase, icono, texto) => z.innerHTML = `<div class=\"aviso ${clase}\"><span class=\"ic\">${icono}</span><p>${texto}</p></div>`; pinta(\"\", \"…\", \"Comprobando si se puede sincronizar…\"); if (typeof SYNC === \"undefined\") return pinta(\"\", \"!\", \"El viaje se guarda en este móvil. Para que lo vea otra persona, expórtalo.\"); try { const e = await SYNC.estado(); if (e.ok) pinta(\"ok\", \"✓\", `Se guarda aquí y se sube a la nube. Pilar lo verá en su móvil en cuanto abra la app, sin hacer nada.`); else if (e.login) pinta(\"\", \"!\", `Sin sesión: por ahora solo se guarda en este móvil. Entra desde <a href=\"../\">Viajes</a> para que lo vea Pilar.`); else pinta(\"\", \"!\", `Sin conexión: se guarda aquí y subirá solo cuando haya cobertura.`); } catch { pinta(\"\", \"!\", \"El viaje se guarda en este móvil.\"); } } pintaDias(); pintaReservas(); setTimeout(pintaAvisoNube, 300); sucio(); } catch { eti.textContent = \"+ Añadir una foto\"; alert(\"No se pudo leer la imagen.\"); } }); }); document.querySelectorAll(\"[data-quitar-foto]\").forEach(b => { if (b.dataset.l) return; b.dataset.l = 1; b.addEventListener(\"click\", () => { delete viaje.dias[+b.dataset.quitarFoto].foto; async function pintaAvisoNube(){ const z = document.getElementById(\"aviso-nube\"); if (!z) return; const pinta = (clase, icono, texto) => z.innerHTML = `<div class=\"aviso ${clase}\"><span class=\"ic\">${icono}</span><p>${texto}</p></div>`; pinta(\"\", \"…\", \"Comprobando si se puede sincronizar…\"); if (typeof SYNC === \"undefined\") return pinta(\"\", \"!\", \"El viaje se guarda en este móvil. Para que lo vea otra persona, expórtalo.\"); try { const e = await SYNC.estado(); if (e.ok) pinta(\"ok\", \"✓\", `Se guarda aquí y se sube a la nube. Pilar lo verá en su móvil en cuanto abra la app, sin hacer nada.`); else if (e.login) pinta(\"\", \"!\", `Sin sesión: por ahora solo se guarda en este móvil. Entra desde <a href=\"../\">Viajes</a> para que lo vea Pilar.`); else pinta(\"\", \"!\", `Sin conexión: se guarda aquí y subirá solo cuando haya cobertura.`); } catch { pinta(\"\", \"!\", \"El viaje se guarda en este móvil.\"); } } pintaDias(); pintaReservas(); setTimeout(pintaAvisoNube, 300); sucio(); }); }); } /* ---- Buscar un sitio en Wikipedia ---- */ async function buscarSitio(texto, fuente = \"wikipedia\"){ const base = fuente === \"wikivoyage\" ? \"https://es.wikivoyage.org/w/api.php\" : \"https://es.wikipedia.org/w/api.php\"; const url = base + \"?\" + new URLSearchParams({ action:\"query\", format:\"json\", origin:\"*\", generator:\"search\", gsrsearch:texto, gsrlimit:\"3\", prop:\"extracts|pageimages|coordinates\", exintro:\"1\", explaintext:\"1\", exsentences:\"3\", piprop:\"thumbnail\", pithumbsize:\"640\" }); const ctrl = new AbortController(); const corte = setTimeout(() => ctrl.abort(), 12000); try { const r = await fetch(url, { signal: ctrl.signal }); clearTimeout(corte); if (!r.ok) throw new Error(\"respuesta \" + r.status); const d = await r.json(); const paginas = d?.query?.pages; if (!paginas) return []; return Object.values(paginas) .sort((a,b) => (a.index||0) - (b.index||0)) .map(p => ({ titulo: p.title, texto: (p.extract || \"\").trim(), imagen: p.thumbnail?.source || \"\", xy: p.coordinates?.[0] ? `${p.coordinates[0].lat},${p.coordinates[0].lon}` : \"\" })) .filter(p => p.texto || p.imagen); } catch (e){ clearTimeout(corte); throw e; } } function abreBuscador(i){ const z = document.getElementById(\"bz-\" + i); if (z.dataset.abierto){ z.innerHTML = \"\"; delete z.dataset.abierto; return; } z.dataset.abierto = \"1\"; z.innerHTML = `<div class=\"busca-caja\"> <input type=\"text\" class=\"mini\" id=\"q-${i}\" placeholder=\"Lago Bled, Cudillero, Somiedo…\"> <div class=\"fuentes\" id=\"fu-${i}\"> <button class=\"fuente on\" data-fuente=\"wikipedia\">Qué es</button> <button class=\"fuente\" data-fuente=\"wikivoyage\">Qué hacer</button> </div> <div class=\"btns\" style=\"margin-top:8px\"> <button class=\"btn fino solid\" id=\"go-${i}\">Buscar</button> </div> <div id=\"rs-${i}\"></div> </div>`; z.querySelectorAll(\".fuente\").forEach(b => b.addEventListener(\"click\", () => { z.querySelectorAll(\".fuente\").forEach(x => x.classList.toggle(\"on\", x === b)); })); const lanzar = async () => { const q = document.getElementById(\"q-\" + i).value.trim(); if (!q) return; const rs = document.getElementById(\"rs-\" + i); const fuente = document.querySelector(`#fu-${i} .fuente.on`)?.dataset.fuente || \"wikipedia\"; rs.innerHTML = `<p class=\"cargando\">Buscando en ${fuente === \"wikivoyage\" ? \"Wikivoyage\" : \"Wikipedia\"}…</p>`; try { const res = await buscarSitio(q, fuente); if (!res.length){ rs.innerHTML = `<p class=\"cargando\">Nada con ese nombre${ fuente === \"wikivoyage\" ? \" en la guía de viajes. Prueba con «Qué es».\" : \"\"}.</p>`; return; } rs.innerHTML = res.map((r, k) => ` <div class=\"res\"> ${r.imagen ? `<img src=\"${r.imagen}\" alt=\"\">` : \"\"} <h4>${esc(r.titulo)}</h4> <p>${esc(r.texto || \"Sin descripción.\")}</p> <div class=\"btns\"> <button class=\"btn fino solid\" data-usar=\"${i}:${k}\">Añadir como parada</button> ${r.imagen ? `<button class=\"btn fino\" data-usar-img=\"${i}:${k}\">Usar la foto</button>` : \"\"} </div> </div>`).join(\"\"); rs.dataset.res = JSON.stringify(res); engancharResultados(); } catch { rs.innerHTML = `<p class=\"cargando\">No se pudo consultar. ¿Hay cobertura?</p>`; } }; document.getElementById(\"go-\" + i).addEventListener(\"click\", lanzar); document.getElementById(\"q-\" + i).addEventListener(\"keydown\", e => { if (e.key === \"Enter\"){ e.preventDefault(); lanzar(); } }); } function engancharResultados(){ document.querySelectorAll(\"[data-usar]\").forEach(b => { if (b.dataset.l) return; b.dataset.l = 1; b.addEventListener(\"click\", () => { const [i, k] = b.dataset.usar.split(\":\").map(Number); const res = JSON.parse(document.getElementById(\"rs-\" + i).dataset.res)[k]; viaje.dias[i].paradas.push({ h:\"\", txt:res.titulo, n:res.texto.slice(0, 240), mapa:res.titulo }); async function pintaAvisoNube(){ const z = document.getElementById(\"aviso-nube\"); if (!z) return; const pinta = (clase, icono, texto) => z.innerHTML = `<div class=\"aviso ${clase}\"><span class=\"ic\">${icono}</span><p>${texto}</p></div>`; pinta(\"\", \"…\", \"Comprobando si se puede sincronizar…\"); if (typeof SYNC === \"undefined\") return pinta(\"\", \"!\", \"El viaje se guarda en este móvil. Para que lo vea otra persona, expórtalo.\"); try { const e = await SYNC.estado(); if (e.ok) pinta(\"ok\", \"✓\", `Se guarda aquí y se sube a la nube. Pilar lo verá en su móvil en cuanto abra la app, sin hacer nada.`); else if (e.login) pinta(\"\", \"!\", `Sin sesión: por ahora solo se guarda en este móvil. Entra desde <a href=\"../\">Viajes</a> para que lo vea Pilar.`); else pinta(\"\", \"!\", `Sin conexión: se guarda aquí y subirá solo cuando haya cobertura.`); } catch { pinta(\"\", \"!\", \"El viaje se guarda en este móvil.\"); } } pintaDias(); pintaReservas(); setTimeout(pintaAvisoNube, 300); sucio(); }); }); document.querySelectorAll(\"[data-usar-img]\").forEach(b => { if (b.dataset.l) return; b.dataset.l = 1; b.addEventListener(\"click\", async () => { const [i, k] = b.dataset.usarImg.split(\":\").map(Number); const res = JSON.parse(document.getElementById(\"rs-\" + i).dataset.res)[k]; b.textContent = \"Guardando…\"; try { const r = await fetch(res.imagen); const blob = await r.blob(); viaje.dias[i].foto = await comprimir(blob); async function pintaAvisoNube(){ const z = document.getElementById(\"aviso-nube\"); if (!z) return; const pinta = (clase, icono, texto) => z.innerHTML = `<div class=\"aviso ${clase}\"><span class=\"ic\">${icono}</span><p>${texto}</p></div>`; pinta(\"\", \"…\", \"Comprobando si se puede sincronizar…\"); if (typeof SYNC === \"undefined\") return pinta(\"\", \"!\", \"El viaje se guarda en este móvil. Para que lo vea otra persona, expórtalo.\"); try { const e = await SYNC.estado(); if (e.ok) pinta(\"ok\", \"✓\", `Se guarda aquí y se sube a la nube. Pilar lo verá en su móvil en cuanto abra la app, sin hacer nada.`); else if (e.login) pinta(\"\", \"!\", `Sin sesión: por ahora solo se guarda en este móvil. Entra desde <a href=\"../\">Viajes</a> para que lo vea Pilar.`); else pinta(\"\", \"!\", `Sin conexión: se guarda aquí y subirá solo cuando haya cobertura.`); } catch { pinta(\"\", \"!\", \"El viaje se guarda en este móvil.\"); } } pintaDias(); pintaReservas(); setTimeout(pintaAvisoNube, 300); sucio(); } catch { b.textContent = \"No se pudo guardar\"; } }); }); } /* ---- Preparar la petición para una IA ---- */ function plantilla(d){ return `Móntame un viaje bien organizado y devuélvemelo en Markdown, con el formato exacto de abajo. DESTINO: ${d.destino} DÍAS: ${d.dias} SALGO Y VUELVO A: ${d.salida || \"sin especificar\"} CÓMO VIAJO: ${d.como || \"sin especificar\"} QUÉ ME INTERESA: ${d.interes || \"un poco de todo\"} RITMO: ${d.ritmo} MÁXIMO AL VOLANTE: unas ${d.coche} horas al día ${d.extra ? \"OTRAS COSAS: \" + d.extra : \"\"} ${(d.ida || d.vuelta || d.cocheAlq) ? ` ═══ ESTO YA ESTÁ RESERVADO, NO LO CAMBIES ═══ ${d.ida ? \"IDA: \" + d.ida : \"\"} ${d.vuelta ? \"VUELTA: \" + d.vuelta : \"\"} ${d.cocheAlq ? \"COCHE DE ALQUILER: \" + d.cocheAlq : \"\"} Construye el viaje ALREDEDOR de esos horarios: - Ponlos como paradas reales, con su hora, el día que toquen. - Si la ida llega tarde, el primer día es solo llegar y dormir cerca. - Si la vuelta sale de madrugada, la última noche se duerme cerca del aeropuerto y el día anterior se deja sin nada que no se pueda saltar. - Si el coche se recoge o se devuelve a una hora, cuadra el día para llegar con margen, y ten en cuenta lo que se puede o no hacer sin coche antes y después. - Avísame por escrito si algo de lo que planeo choca con esos horarios. ` : \"\"} ═══ CÓMO QUIERO QUE ESTÉ ORGANIZADO ═══ 1. RECORRIDO COHERENTE Traza un círculo, en un sentido o en el otro, o una línea limpia. Nunca ir y volver por el mismo tramo, ni cruzar la zona de lado a lado dos veces. Cada día debe dejarme más cerca del siguiente. 2. EL ALOJAMIENTO, CON SU NOMBRE En «Alojamiento» pon el nombre tal cual aparece, para poder buscarlo después. Si conoces su web propia, añádela tras un « · ». Si no la sabes con certeza, no la inventes: deja solo el nombre. No pongas enlaces de Booking ni de otros buscadores: la app los genera sola. 3. POCAS BASES, MÁS NOCHES Prioriza quedarte 2 o 3 noches en el mismo alojamiento y salir desde allí, antes que cambiar de sitio cada noche. Cambiar de base cuesta media mañana. Cambia solo cuando el salto geográfico lo justifique de verdad. Los alojamientos, cerca de lo que se visita ese día, no a 40 minutos. 4. TIEMPO AL VOLANTE Ningún día debe pasar mucho de ${d.coche} horas de coche. Escribe en cada día los kilómetros y el tiempo aproximados. Cuenta que en carretera de montaña se va mucho más lento de lo que dice el mapa. 5. AVÍSAME DE LO QUE HAY QUE RESERVAR Cuando una parada tenga visita guiada, entrada con hora, ferri o último teleférico, dilo en la nota para que lo reserve yo con tiempo, y colócala a la hora que tenga sentido dentro del día. 6. UN IMPRESCINDIBLE POR DÍA Marca con ★ al principio la parada que no habría que perderse ese día. 7. RITMO ALTERNO No pongas dos jornadas exigentes seguidas. Después de un día largo, uno suave. Y evita amontonar: entre 2 y 4 paradas de verdad por día. 8. PRIMERO Y ÚLTIMO DÍA REALISTAS El día de llegada, poco y cerca. El de vuelta, con margen de sobra y sin nada que no se pueda saltar. 9. MASIFICACIÓN En los sitios famosos, di a qué hora conviene ir para no coincidir con todos. 10. PLAN B En los días de montaña o de mar, añade al final una línea «Si llueve:» con una alternativa cercana a cubierto o suave. 11. LO PRÁCTICO Dónde llenar la nevera, dónde repostar antes de meterse en zona vacía, aparcamientos que se llenan pronto y si son de pago. 12. NORMAS DEL PAÍS (solo si no es España) Al final, después de los días, añade un apartado «## Normas» con lo que cambia respecto a España y de verdad afecta al día a día. Sé concreto: - Andando por carretera sin acera: ¿por qué lado se camina? - En bici: ¿por dónde se circula? ¿casco obligatorio? ¿luces de día? - En coche: límites de velocidad, luces encendidas de día, dónde no se puede adelantar, prioridad en rotondas y en carreteras de montaña. - Alcohol al volante: tasa permitida. - Peajes o viñeta: cómo se paga y dónde se compra antes de entrar. - Qué hay que llevar obligatoriamente en el coche. - Aparcar: colores de las líneas y qué significan. - Con perro: si puede ir suelto, si hay bozal obligatorio, si entra en transporte público y en los parques naturales. - Teléfonos de emergencia. - Cualquier norma que sorprenda a alguien de España. Si algo ha cambiado hace poco o no estás seguro, dilo en vez de inventarlo. ═══ FORMATO (respétalo, es lo que lee mi app) ═══ # Nombre del viaje Salida: punto de partida ## Día 1 — Título de la jornada Duermes en: pueblo o ciudad Alojamiento: Nombre exacto del hotel o casa · web propia si la sabes Coche: 120 km · 2 h - **Mañana** · ★ Nombre del sitio — Qué es y lo práctico que haya que saber. [Nombre exacto para el GPS] - **Tarde** · Otro sitio — Explicación breve. [Nombre para el GPS] - Si llueve: alternativa a cubierto por la zona. ## Día 2 — Título Duermes en: el mismo sitio si no hay que moverse Coche: 60 km · 1 h REGLAS DE FORMATO: - Exactamente ${d.dias} apartados «## Día». - Cada parada empieza por «- », hora en **negrita**, luego « · », nombre, « — » y la explicación. - Entre corchetes al final, el nombre que un GPS encuentre, con topónimo local si ayuda. - No inventes horarios ni precios exactos si no estás seguro: di «comprobar» en su lugar. - Nada de texto fuera de ese formato: ni introducción ni despedida. - El apartado «## Normas» va al final, con líneas que empiezan por «- ».`; } function abrirPedir(){ const z = document.getElementById(\"zona-pedir\"); if (z.dataset.abierto){ z.innerHTML = \"\"; delete z.dataset.abierto; document.getElementById(\"abrir-pedir\").textContent = \"Preparar la petición\"; return; } z.dataset.abierto = \"1\"; document.getElementById(\"abrir-pedir\").textContent = \"Cerrar\"; z.innerHTML = ` <div class=\"pedir-form\"> <div class=\"campo\"><label>A dónde</label> <input type=\"text\" id=\"p-destino\" placeholder=\"Croacia, Dolomitas, Picos de Europa…\"></div> <div class=\"dos\"> <div class=\"campo\"><label>Cuántos días</label> <input type=\"text\" id=\"p-dias\" value=\"5\" inputmode=\"numeric\"></div> <div class=\"campo\"><label>Desde dónde salís</label> <input type=\"text\" id=\"p-salida\" placeholder=\"San Miguel de las Dueñas\"></div> </div> <div class=\"campo\"><label>Cómo viajáis</label> <input type=\"text\" id=\"p-como\" placeholder=\"En furgo camper, durmiendo en ella, con dos perras\"></div> <p class=\"sub-sec\">Lo que ya está cerrado</p> <p class=\"nota\" style=\"margin:0 0 10px\">Si tenéis vuelos o coche reservado, ponlo aquí: es lo que manda sobre el resto.</p> <div class=\"campo\"><label>Ida</label> <input type=\"text\" id=\"p-ida\" placeholder=\"Vuelo Madrid–Zagreb, sale 18:55, llega 21:30\"></div> <p class=\"nota\" style=\"margin:-4px 0 10px\">Los localizadores y teléfonos se apuntan luego, en «Reservas», abajo del todo.</p> <div class=\"campo\"><label>Vuelta</label> <input type=\"text\" id=\"p-vuelta\" placeholder=\"Vuelo Zagreb–Madrid a las 06:00 del último día\"></div> <div class=\"campo\"><label>Coche de alquiler</label> <input type=\"text\" id=\"p-coche-alq\" placeholder=\"Recojo en el aeropuerto a las 22:00, devuelvo el día 7 a las 18:00\"></div> <p class=\"sub-sec\">Cómo lo queréis</p> <div class=\"campo\"><label>Qué os interesa</label> <input type=\"text\" id=\"p-interes\" placeholder=\"Montaña, rutas a pie, pueblos, comer bien…\"></div> <div class=\"dos\"> <div class=\"campo\"><label>Ritmo</label> <select id=\"p-ritmo\"> <option value=\"tranquilo, sin madrugar y con tiempo en cada sitio\">Tranquilo</option> <option value=\"normal, equilibrado entre ver cosas y disfrutarlas\" selected>Normal</option> <option value=\"intenso, aprovechando el día de sol a sol\">Intenso</option> </select></div> <div class=\"campo\"><label>Horas de coche al día</label> <select id=\"p-coche\"> <option value=\"1,5\">Poco · 1,5 h</option> <option value=\"2,5\" selected>Normal · 2,5 h</option> <option value=\"4\">Me da igual · 4 h</option> </select></div> </div> <div class=\"campo\"><label>Algo más</label> <input type=\"text\" id=\"p-extra\" placeholder=\"En agosto, evitar masificación, vamos con perros…\"></div> <button class=\"btn solid\" id=\"copiar-pedir\" style=\"width:100%\">Copiar la petición</button> <ol class=\"pasos\"> <li><b>Pega la petición</b> en Claude, ChatGPT o Gemini.</li> <li><b>Copia la respuesta</b> entera, tal cual venga.</li> <li>Vuelve aquí y dale a <b>Importar</b>, abajo del todo.</li> </ol> </div>`; document.getElementById(\"copiar-pedir\").addEventListener(\"click\", async () => { const v = id => document.getElementById(id).value.trim(); if (!v(\"p-destino\")){ alert(\"Dime al menos a dónde queréis ir.\"); return; } const texto = plantilla({ destino:v(\"p-destino\"), dias:v(\"p-dias\") || \"5\", salida:v(\"p-salida\"), como:v(\"p-como\"), interes:v(\"p-interes\"), extra:v(\"p-extra\"), ritmo:v(\"p-ritmo\"), coche:v(\"p-coche\"), ida:v(\"p-ida\"), vuelta:v(\"p-vuelta\"), cocheAlq:v(\"p-coche-alq\") }); const b = document.getElementById(\"copiar-pedir\"); try { if (navigator.share) await navigator.share({ text: texto }); else { await navigator.clipboard.writeText(texto); } b.textContent = \"Copiado. Pégalo en la IA.\"; setTimeout(() => { b.textContent = \"Copiar la petición\"; }, 3000); } catch {} }); } document.getElementById(\"abrir-pedir\").addEventListener(\"click\", abrirPedir); document.getElementById(\"add-dia\").addEventListener(\"click\", () => { viaje.dias.push({ t:\"\", dest:\"\", paradas:[{h:\"\", txt:\"\", n:\"\", mapa:\"\"}] }); async function pintaAvisoNube(){ const z = document.getElementById(\"aviso-nube\"); if (!z) return; const pinta = (clase, icono, texto) => z.innerHTML = `<div class=\"aviso ${clase}\"><span class=\"ic\">${icono}</span><p>${texto}</p></div>`; pinta(\"\", \"…\", \"Comprobando si se puede sincronizar…\"); if (typeof SYNC === \"undefined\") return pinta(\"\", \"!\", \"El viaje se guarda en este móvil. Para que lo vea otra persona, expórtalo.\"); try { const e = await SYNC.estado(); if (e.ok) pinta(\"ok\", \"✓\", `Se guarda aquí y se sube a la nube. Pilar lo verá en su móvil en cuanto abra la app, sin hacer nada.`); else if (e.login) pinta(\"\", \"!\", `Sin sesión: por ahora solo se guarda en este móvil. Entra desde <a href=\"../\">Viajes</a> para que lo vea Pilar.`); else pinta(\"\", \"!\", `Sin conexión: se guarda aquí y subirá solo cuando haya cobertura.`); } catch { pinta(\"\", \"!\", \"El viaje se guarda en este móvil.\"); } } pintaDias(); pintaReservas(); setTimeout(pintaAvisoNube, 300); sucio(); const ds = document.querySelectorAll(\".dia\"); ds[ds.length-1]?.scrollIntoView?.({behavior:\"smooth\", block:\"center\"}); }); /* ---- Cabecera ---- */ [\"nombre\",\"desde\",\"hasta\",\"salida\"].forEach(id => document.getElementById(id).addEventListener(\"input\", e => { viaje[id] = e.target.value; if (id === \"nombre\") document.getElementById(\"titulo\").textContent = e.target.value.trim() || \"Nuevo viaje\"; sucio(); })); let pendiente = false; function sucio(){ pendiente = true; document.getElementById(\"estado\").textContent = \"Hay cambios sin guardar.\"; } /* ---- Guardar ---- */ function guardar(){ if (!viaje.nombre.trim()){ alert(\"Ponle un nombre al viaje antes de guardar.\"); document.getElementById(\"nombre\").focus(); return false; } viaje.actualizado = new Date().toISOString(); const todos = leerTodos(); const i = todos.findIndex(v => v.id === viaje.id); if (i >= 0) todos[i] = viaje; else todos.push(viaje); if (!guardarTodos(todos)){ alert(\"No se pudo guardar en este móvil.\"); return false; } pendiente = false; const e = document.getElementById(\"estado\"); e.innerHTML = `<span class=\"ok\">Guardado.</span> Ya aparece en la portada.`; history.replaceState(null, \"\", \"?id=\" + viaje.id); pintaAvisoNube(); if (typeof SYNC !== \"undefined\") SYNC.subir(viaje).then(ok => { e.innerHTML = ok ? `<span class=\"ok\">Guardado y sincronizado.</span> Ya lo ve el otro móvil.` : `<span class=\"ok\">Guardado en este móvil.</span> Se subirá cuando haya conexión.`; }).catch(()=>{}); return true; } document.getElementById(\"guardar\").addEventListener(\"click\", guardar); document.getElementById(\"previsualizar\").addEventListener(\"click\", () => { if (guardar()) location.href = \"../viaje/?id=\" + viaje.id; }); /* ---- Exportar e importar ---- */ async function compartir(texto, titulo){ try { if (navigator.share) await navigator.share({ title: titulo, text: texto }); else { await navigator.clipboard.writeText(texto); alert(\"Copiado al portapapeles.\"); } } catch {} } document.getElementById(\"exportar\").addEventListener(\"click\", () => { guardar(); const z = document.getElementById(\"zona-exportar\"); if (z.dataset.abierto){ z.innerHTML = \"\"; delete z.dataset.abierto; return; } z.dataset.abierto = \"1\"; z.innerHTML = `<div class=\"busca-caja\"> <p class=\"nota\" style=\"margin:0 0 10px\">¿Para qué lo quieres?</p> <div class=\"btns\"> <button class=\"btn fino solid\" id=\"ex-leer\">Para leer o imprimir</button> <button class=\"btn fino\" id=\"ex-copia\">Copia de seguridad</button> </div> <p class=\"nota\" style=\"margin-top:10px\">Lo primero sale como texto con formato, listo para pegar en Notas o mandar por WhatsApp. Lo segundo es el archivo exacto, para volver a importarlo tal cual.</p> </div>`; document.getElementById(\"ex-leer\").addEventListener(\"click\", () => compartir(aMarkdown(viaje), viaje.nombre || \"Viaje\")); document.getElementById(\"ex-copia\").addEventListener(\"click\", () => compartir(JSON.stringify(viaje, null, 2), viaje.nombre || \"Viaje\")); }); /* ---- El viaje, como documento legible ---- */ function aMarkdown(v){ const L = [`# ${v.nombre || \"Viaje\"}`]; if (v.salida) L.push(`Salida: ${v.salida}`); if (v.desde) L.push(`Fechas: ${v.desde}${v.hasta ? \" a \" + v.hasta : \"\"}`); L.push(\"\"); (v.dias || []).forEach((d, i) => { L.push(`## Día ${i+1} — ${d.t || \"Sin título\"}`); if (d.dest) L.push(`Duermes en: ${d.dest}`); if (d.hotel) L.push(`Alojamiento: ${d.hotel}${d.hotelWeb ? \" · \" + d.hotelWeb : \"\"}`); if (d.km) L.push(`Coche: ${d.km}`); L.push(\"\"); (d.paradas || []).forEach(p => { let l = \"- \"; if (p.h) l += `**${p.h}** · `; if (p.key) l += \"★ \"; l += p.txt || \"\"; if (p.n) l += ` — ${p.n}`; if (p.mapa && p.mapa !== p.txt) l += ` [${p.mapa}]`; L.push(l); }); if (d.lluvia) L.push(`- Si llueve: ${d.lluvia}`); L.push(\"\"); }); if ((v.normas || []).length){ L.push(\"## Normas\"); v.normas.forEach(n => L.push(`- ${n}`)); L.push(\"\"); } return L.join(\"\\n\"); } /* ---- Importar: aguanta lo que devuelva cualquier IA ---- */ function extraerJSON(texto){ let t = String(texto || \"\").trim(); // quita bloques de código y preámbulos del tipo \"Aquí tienes tu viaje:\" const bloque = t.match(/```(?:json)?\\s*([\\s\\S]*?)```/i); if (bloque) t = bloque[1].trim(); const abre = t.indexOf(\"{\"), cierra = t.lastIndexOf(\"}\"); if (abre === -1 || cierra === -1 || cierra < abre) return null; t = t.slice(abre, cierra + 1); try { return JSON.parse(t); } catch {} // último intento: comillas tipográficas y comas colgando try { return JSON.parse(t.replace(/[\\u201C\\u201D]/g, '\"').replace(/[\\u2018\\u2019]/g, \"'\") .replace(/,\\s*([}\\]])/g, \"$1\")); } catch { return null; } } function leerMarkdown(texto){ const lineas = String(texto || \"\").split(\"\\n\"); const v = { nombre:\"\", salida:\"\", desde:\"\", hasta:\"\", dias:[], normas:[] }; let dia = null, enNormas = false; for (let cruda of lineas){ const l = cruda.trim(); if (!l) continue; // # Nombre del viaje let m = l.match(/^#\\s+(.+)$/); if (m && !l.startsWith(\"##\")){ v.nombre = m[1].trim(); continue; } // ## Normas: no es un día, es información del país if (/^##+\\s*(normas|conducci[óo]n|circulaci[óo]n)/i.test(l)){ dia = null; enNormas = true; continue; } if (enNormas){ const n = l.match(/^[-*•]\\s+(.*)$/); if (n) v.normas.push(n[1].replace(/\\*/g,\"\").trim()); continue; } // ## Día 1 — Título m = l.match(/^##+\\s*(?:d[ií]a\\s*\\d+\\s*[—–\\-:.]?\\s*)?(.*)$/i); if (m && /^##/.test(l)){ dia = { t: m[1].trim(), dest:\"\", paradas:[] }; v.dias.push(dia); continue; } // Salida: / Duermes en: m = l.match(/^\\**\\s*salida\\s*:\\**\\s*(.+)$/i); if (m){ v.salida = m[1].replace(/\\*/g,\"\").trim(); continue; } m = l.match(/^\\**\\s*(?:duermes?\\s+en|noche)\\s*:\\**\\s*(.+)$/i); if (m && dia){ dia.dest = m[1].replace(/\\*/g,\"\").trim(); continue; } m = l.match(/^\\**\\s*(?:coche|conducci[óo]n|trayecto|km)\\s*:\\**\\s*(.+)$/i); if (m && dia){ dia.km = m[1].replace(/\\*/g,\"\").trim(); continue; } m = l.match(/^\\**\\s*(?:alojamiento|hotel|d[óo]nde\\s+dormir)\\s*:\\**\\s*(.+)$/i); if (m && dia){ const partes = m[1].replace(/\\*/g,\"\").split(\"·\").map(x => x.trim()); dia.hotel = partes[0]; const web = partes.slice(1).find(x => /^https?:\\/\\//i.test(x)); if (web) dia.hotelWeb = web; continue; } // - **Hora** · Sitio — Nota [GPS] m = l.match(/^[-*•]\\s+(.*)$/); if (m && dia){ let resto = m[1]; let h = \"\", mapa = \"\", n = \"\"; const gps = resto.match(/\\[([^\\]]+)\\]\\s*$/); if (gps){ mapa = gps[1].trim(); resto = resto.slice(0, gps.index).trim(); } const hora = resto.match(/^\\*\\*([^*]+)\\*\\*\\s*[·:.\\-–—]?\\s*/); if (hora){ h = hora[1].trim(); resto = resto.slice(hora[0].length); } // \"Si llueve:\" es plan B del día, no una parada const lluvia = resto.match(/^\\**\\s*si\\s+llueve\\s*:?\\**\\s*(.+)$/i); if (lluvia){ dia.lluvia = lluvia[1].replace(/\\*/g,\"\").trim(); continue; } const corte = resto.split(/\\s+[—–]\\s+|\\s+-\\s+/); let txt = (corte.shift() || \"\").replace(/\\*/g,\"\").trim(); n = corte.join(\" — \").replace(/\\*/g,\"\").trim(); const clave = /^[★*]\\s*/.test(txt); txt = txt.replace(/^[★*]\\s*/, \"\"); if (txt) dia.paradas.push({ h, txt, n, mapa: mapa || txt, key: clave || undefined }); } } v.dias = v.dias.filter(d => d.paradas.length || d.t); return v.dias.length ? v : null; } function normalizar(v){ const texto = x => typeof x === \"string\" ? x : (x == null ? \"\" : String(x)); const dias = Array.isArray(v.dias) ? v.dias : []; return { id: nuevoId(), nombre: texto(v.nombre || v.titulo || \"Viaje sin nombre\"), desde: texto(v.desde || \"\"), hasta: texto(v.hasta || \"\"), salida: texto(v.salida || v.origen || \"\"), normas: Array.isArray(v.normas) ? v.normas.map(texto).filter(Boolean) : [], actualizado: new Date().toISOString(), dias: dias.map(d => ({ t: texto(d.t || d.titulo || d.nombre || \"\"), dest: texto(d.dest || d.destino || d.duerme || \"\"), km: texto(d.km || \"\"), hotel: texto(d.hotel || \"\"), hotelWeb: texto(d.hotelWeb || \"\"), lluvia: texto(d.lluvia || \"\"), paradas: (Array.isArray(d.paradas) ? d.paradas : []).map(p => ({ h: texto(p.h || p.hora || \"\"), txt: texto(p.txt || p.texto || p.nombre || p.titulo || \"\"), n: texto(p.n || p.nota || p.descripcion || \"\"), mapa: texto(p.mapa || p.gps || p.buscar || p.txt || p.nombre || \"\"), key: !!p.key })).filter(p => p.txt) })) }; } document.getElementById(\"importar\").addEventListener(\"click\", () => { const txt = prompt(\"Pega aquí lo que te haya devuelto la IA (o un viaje exportado):\"); if (!txt) return; const crudo = extraerJSON(txt) || leerMarkdown(txt); if (!crudo){ alert(\"No he encontrado ningún viaje en ese texto.\\n\\nCopia la respuesta entera de la IA, desde el título hasta el último día.\"); return; } const v = normalizar(crudo); if (!v.dias.length){ alert(\"He leído el texto pero no trae ningún día con paradas.\"); return; } const paradas = v.dias.reduce((n,d) => n + d.paradas.length, 0); if (!confirm(`Voy a importar «${v.nombre}»: ${v.dias.length} días y ${paradas} paradas.\\n\\n¿Lo añado?`)) return; const todos = leerTodos(); todos.push(v); guardarTodos(todos); if (typeof SYNC !== \"undefined\") SYNC.subir(v).catch(()=>{}); location.href = \"?id=\" + v.id; }); document.getElementById(\"eliminar\").addEventListener(\"click\", async () => { if (!confirm(\"¿Eliminar este viaje del móvil? No se puede deshacer.\")) return; guardarTodos(leerTodos().filter(v => v.id !== viaje.id)); if (typeof SYNC !== \"undefined\") { try { await SYNC.borrar(viaje.id); } catch {} } location.href = \"../\"; }); window.addEventListener(\"beforeunload\", e => { if (pendiente){ e.preventDefault(); e.returnValue = \"\"; } }); async function pintaAvisoNube(){ const z = document.getElementById(\"aviso-nube\"); if (!z) return; const pinta = (clase, icono, texto) => z.innerHTML = `<div class=\"aviso ${clase}\"><span class=\"ic\">${icono}</span><p>${texto}</p></div>`; pinta(\"\", \"…\", \"Comprobando si se puede sincronizar…\"); if (typeof SYNC === \"undefined\") return pinta(\"\", \"!\", \"El viaje se guarda en este móvil. Para que lo vea otra persona, expórtalo.\"); try { const e = await SYNC.estado(); if (e.ok) pinta(\"ok\", \"✓\", `Se guarda aquí y se sube a la nube. Pilar lo verá en su móvil en cuanto abra la app, sin hacer nada.`); else if (e.login) pinta(\"\", \"!\", `Sin sesión: por ahora solo se guarda en este móvil. Entra desde <a href=\"../\">Viajes</a> para que lo vea Pilar.`); else pinta(\"\", \"!\", `Sin conexión: se guarda aquí y subirá solo cuando haya cobertura.`); } catch { pinta(\"\", \"!\", \"El viaje se guarda en este móvil.\"); } } TEMA.aplica(); TEMA.pintaMini(); pintaDias(); pintaReservas(); setTimeout(pintaAvisoNube, 300); if (\"serviceWorker\" in navigator){ window.addEventListener(\"load\", async () => { try { const reg = await navigator.serviceWorker.register(\"sw.js\", { updateViaCache: \"none\" }); reg.update(); // Recargar no basta: el service worker viejo sigue mandando hasta que se // cierran todas las pestañas. Hay que pedirle al nuevo que tome el mando. const vigila = sw => sw && sw.addEventListener(\"statechange\", () => { if (sw.state === \"installed\" && navigator.serviceWorker.controller) avisaNueva(reg); }); if (reg.waiting) avisaNueva(reg); vigila(reg.installing); reg.addEventListener(\"updatefound\", () => vigila(reg.installing)); let yaRecargado = false; navigator.serviceWorker.addEventListener(\"controllerchange\", () => { if (yaRecargado) return; yaRecargado = true; location.reload(); }); } catch {} }); function avisaNueva(reg){ if (document.getElementById(\"hay-nueva\")) return; const b = document.createElement(\"button\"); b.id = \"hay-nueva\"; b.className = \"aviso-nueva\"; b.textContent = \"Versión nueva · tocar para usarla\"; b.addEventListener(\"click\", () => { b.textContent = \"Actualizando…\"; reg.waiting?.postMessage({ tipo: \"aplicar\" }); setTimeout(() => location.reload(), 900); }); document.body.appendChild(b); } } </script>"
}